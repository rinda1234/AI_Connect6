[project]
name = "connect6-student-package"
version = "0.1.0"
description = "Student distribution package for 육목(connect-6) assignment"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
  "numpy>=2.0.0",
]

[tool.uv]
package = false
